// This file exists for legacy compatibility only.
// The auth callback is handled at /auth/confirm/page.tsx
// Static export mode (output: 'export') does not support API route handlers.
// Keeping this file empty prevents build errors while maintaining the file structure.
export const dynamic = undefined;
