'use client';
import React,{useEffect,useMemo,useState}from'react';
import{createClient}from'@/lib/supabase/client';
import AppImage from'@/components/ui/AppImage';
import{MapPinIcon,PhoneIcon,EnvelopeIcon,GlobeAltIcon,CalendarDaysIcon,ShoppingCartIcon,ChatBubbleLeftEllipsisIcon,BuildingOfficeIcon}from'@heroicons/react/24/outline';
import{CheckBadgeIcon,StarIcon as StarSolid}from'@heroicons/react/24/solid';

type Enterprise={id:string;name:string;slug:string|null;logo_url:string|null;cover_url:string|null;category:string|null;city:string|null;address:string|null;description:string|null;phone:string|null;email:string|null;website:string|null;employee_count:number|null;founded_year:number|null;is_verified:boolean|null};
type Product={id:string;name:string;price:string;unit:string|null;category:string|null;image_url:string|null;stock_status:string|null};
const FALLBACK_COVER='https://images.unsplash.com/photo-1497366754035-f200968a6e72?auto=format&fit=crop&w=1400&q=80';
const FALLBACK_LOGO='/assets/images/app_logo.png';

export default function EnterpriseDetailClient({slug}:{slug:string}){
 const supabase=createClient();const[e,setE]=useState<Enterprise|null>(null);const[p,setP]=useState<Product[]>([]);const[loading,setLoading]=useState(true);const[error,setError]=useState('');const[tab,setTab]=useState<'products'|'reviews'|'contact'>('products');const[name,setName]=useState('');const[email,setEmail]=useState('');const[message,setMessage]=useState('');const[sent,setSent]=useState(false);
 useEffect(()=>{let mounted=true;(async()=>{setLoading(true);setError('');const{data,error:q}=await supabase.from('enterprises').select('id,name,slug,logo_url,cover_url,category,city,address,description,phone,email,website,employee_count,founded_year,is_verified').eq('slug',slug).eq('is_active',true).maybeSingle();if(q){if(mounted)setError('Impossible de charger cette entreprise.');setLoading(false);return}if(!data){if(mounted)setError('Entreprise introuvable.');setLoading(false);return}const{data:products}=await supabase.from('products').select('id,name,price,unit,category,image_url,stock_status').eq('enterprise_id',data.id).eq('is_active',true).order('created_at',{ascending:false});if(mounted){setE(data as Enterprise);setP((products??[]) as Product[]);setLoading(false)}})();return()=>{mounted=false}},[slug]);
 const initials=useMemo(()=>e?.name?.slice(0,2).toUpperCase()||'EN',[e]);const cover=e?.cover_url&&!e.cover_url.startsWith('blob:')?e.cover_url:FALLBACK_COVER;const logo=e?.logo_url&&!e.logo_url.startsWith('blob:')?e.logo_url:FALLBACK_LOGO;
 if(loading)return <div className="min-h-[60vh] flex items-center justify-center text-sm text-muted-foreground">Chargement de l’entreprise...</div>;
 if(error||!e)return <div className="max-w-3xl mx-auto py-20 text-center"><p className="text-lg font-bold text-foreground">{error||'Entreprise introuvable.'}</p><p className="text-sm text-muted-foreground mt-2">Vérifiez le lien depuis la liste des entreprises.</p></div>;
 const send=async(x:React.FormEvent)=>{x.preventDefault();if(!name.trim()||!email.trim()||!message.trim())return;setSent(true)};
 return <div className="min-h-screen bg-background"><main className="pt-2"><div className="relative h-48 sm:h-64 overflow-hidden"><AppImage src={cover} alt={`Couverture de ${e.name}`} fill className="object-cover" sizes="100vw"/><div className="absolute inset-0 bg-gradient-to-t from-background via-background/40 to-transparent"/></div><div className="max-w-7xl mx-auto px-4"><div className="relative -mt-16 mb-6 flex flex-col sm:flex-row sm:items-end gap-4"><div className="w-24 h-24 rounded-2xl overflow-hidden border-4 border-background bg-card shadow-2xl shrink-0"><AppImage src={logo} alt={`Logo de ${e.name}`} width={96} height={96} className="object-cover w-full h-full"/></div><div className="flex-1 pb-2"><div className="flex flex-wrap items-center gap-2 mb-1"><h1 className="text-xl font-extrabold text-foreground">{e.name}</h1>{e.is_verified&&<span className="flex items-center gap-1 text-xs font-bold text-primary bg-primary/10 border border-primary/20 px-2 py-0.5 rounded-full"><CheckBadgeIcon className="w-3.5 h-3.5"/>Vérifié</span>}</div><div className="flex flex-wrap items-center gap-4 text-xs text-muted-foreground"><span className="text-primary font-semibold">{e.category||'Entreprise'}</span>{e.city&&<span className="flex items-center gap-1"><MapPinIcon className="w-3.5 h-3.5"/>{e.city}</span>}{e.founded_year&&<span className="flex items-center gap-1"><CalendarDaysIcon className="w-3.5 h-3.5"/>Depuis {e.founded_year}</span>}{e.employee_count!==null&&<span className="flex items-center gap-1"><BuildingOfficeIcon className="w-3.5 h-3.5"/>{e.employee_count} employés</span>}</div></div><div className="flex gap-2 pb-2"><button onClick={()=>{setTab('contact');window.scrollTo({top:document.body.scrollHeight,behavior:'smooth'})}} className="flex items-center gap-2 px-4 py-2 bg-primary text-primary-foreground rounded-xl text-sm font-bold hover:bg-primary/90 transition-colors"><ChatBubbleLeftEllipsisIcon className="w-4 h-4"/>Contacter</button></div></div><div className="grid grid-cols-3 sm:grid-cols-4 gap-3 mb-8">{[{label:'Produits',value:p.length.toString()},{label:'Note',value:'—'},{label:'Avis',value:'0'},{label:'Fondée en',value:e.founded_year?.toString()||'—'}].map((s,i)=><div key={i} className="bg-card border border-border rounded-xl p-3 text-center"><p className="text-lg font-extrabold text-foreground">{s.value}</p><p className="text-[10px] text-muted-foreground">{s.label}</p></div>)}</div><div className="bg-card border border-border rounded-xl p-5 mb-6"><h2 className="text-sm font-extrabold text-foreground mb-2">À propos</h2><p className="text-sm text-muted-foreground leading-relaxed">{e.description||'Aucune description disponible.'}</p></div><div className="flex gap-1 border-b border-border mb-6">{(['products','reviews','contact'] as const).map(x=><button key={x} onClick={()=>setTab(x)} className={`px-5 py-3 text-sm font-semibold border-b-2 transition-colors -mb-px ${tab===x?'border-primary text-primary':'border-transparent text-muted-foreground hover:text-foreground'}`}>{x==='products'?`Produits (${p.length})`:x==='reviews'?'Avis (0)':'Contact'}</button>)}</div>{tab==='products'&&<div className="grid grid-cols-2 sm:grid-cols-3 lg:grid-cols-4 gap-4 mb-10">{p.length?p.map(x=><div key={x.id} className="bg-card border border-border rounded-xl overflow-hidden group"><div className="relative h-40 bg-secondary overflow-hidden">{x.image_url?<img src={x.image_url} alt={x.name} className="w-full h-full object-cover group-hover:scale-105 transition-transform duration-500"/>:<div className="w-full h-full flex items-center justify-center text-2xl font-extrabold text-primary/40">{x.name.slice(0,2).toUpperCase()}</div>}</div><div className="p-3"><h3 className="text-xs font-bold text-foreground line-clamp-2 mb-2">{x.name}</h3><div className="flex items-baseline gap-1"><span className="text-sm font-extrabold text-primary">{x.price}</span>{x.unit&&<span className="text-[10px] text-muted-foreground">/ {x.unit}</span>}</div><span className="text-[10px] text-muted-foreground">{x.stock_status||'Disponibilité non renseignée'}</span></div></div>):<div className="col-span-full text-center py-14 border border-dashed border-border rounded-xl text-sm text-muted-foreground">Cette entreprise n’a pas encore publié de produit.</div>}</div>}{tab==='reviews'&&<div className="bg-card border border-border rounded-xl p-8 mb-10 text-center"><StarSolid className="w-8 h-8 text-primary mx-auto mb-2"/><p className="font-bold text-foreground">Aucun avis pour le moment</p><p className="text-xs text-muted-foreground mt-1">Les avis apparaîtront ici lorsque les clients pourront évaluer cette entreprise.</p></div>}{tab==='contact'&&<div className="grid grid-cols-1 lg:grid-cols-2 gap-6 mb-10"><div className="space-y-4"><div className="bg-card border border-border rounded-xl p-5"><h3 className="text-sm font-extrabold text-foreground mb-4">Coordonnées</h3><div className="space-y-3">{[{icon:<MapPinIcon className="w-4 h-4 text-primary"/>,label:'Adresse',value:e.address},{icon:<PhoneIcon className="w-4 h-4 text-primary"/>,label:'Téléphone',value:e.phone},{icon:<EnvelopeIcon className="w-4 h-4 text-primary"/>,label:'Email',value:e.email},{icon:<GlobeAltIcon className="w-4 h-4 text-primary"/>,label:'Site web',value:e.website}].filter(x=>x.value).map((x,i)=><div key={i} className="flex items-start gap-3 p-3 bg-secondary rounded-xl">{x.icon}<div><p className="text-[10px] text-muted-foreground uppercase tracking-wide font-semibold">{x.label}</p><p className="text-sm font-semibold text-foreground break-all">{x.value}</p></div></div>)}</div></div><div className="bg-card border border-border rounded-xl p-5"><div className="h-32 bg-secondary rounded-xl flex items-center justify-center"><MapPinIcon className="w-7 h-7 text-primary mr-2"/><span className="text-sm text-muted-foreground">{e.city||e.address||'Localisation non renseignée'}</span></div></div></div><div className="bg-card border border-border rounded-xl p-5"><h3 className="text-sm font-extrabold text-foreground mb-4">Envoyer un message</h3>{sent?<div className="text-center py-12"><div className="text-3xl mb-3">✅</div><p className="font-bold text-foreground">Message prêt à être envoyé</p><p className="text-xs text-muted-foreground mt-1">Le formulaire est enregistré côté interface. La messagerie temps réel pourra être branchée ensuite.</p></div>:<form onSubmit={send} className="space-y-4"><label className="block text-xs font-semibold text-muted-foreground uppercase tracking-wide">Votre nom<input required value={name} onChange={x=>setName(x.target.value)} className="mt-1.5 w-full px-3 py-2.5 bg-secondary border border-border rounded-xl text-sm text-foreground focus:outline-none focus:border-primary/60" placeholder="Votre nom"/></label><label className="block text-xs font-semibold text-muted-foreground uppercase tracking-wide">Votre email<input required type="email" value={email} onChange={x=>setEmail(x.target.value)} className="mt-1.5 w-full px-3 py-2.5 bg-secondary border border-border rounded-xl text-sm text-foreground focus:outline-none focus:border-primary/60" placeholder="vous@exemple.cd"/></label><label className="block text-xs font-semibold text-muted-foreground uppercase tracking-wide">Message<textarea required rows={5} value={message} onChange={x=>setMessage(x.target.value)} className="mt-1.5 w-full px-3 py-2.5 bg-secondary border border-border rounded-xl text-sm text-foreground focus:outline-none focus:border-primary/60 resize-none" placeholder="Décrivez votre besoin..."/></label><button className="w-full py-3 bg-primary text-primary-foreground rounded-xl font-bold text-sm hover:bg-primary/90">Envoyer le message</button></form>}</div></div>}</div></main></div>;
}
