'use client';
import React, { useEffect, useMemo, useState } from 'react';
import AppImage from '@/components/ui/AppImage';
import Link from 'next/link';
import AuthGuardModal from '@/components/ui/AuthGuardModal';
import { useAuth } from '@/lib/useAuth';
import { createClient } from '@/lib/supabase/client';
import { MagnifyingGlassIcon, AdjustmentsHorizontalIcon, MapPinIcon, XMarkIcon, PhoneIcon, EnvelopeIcon, BuildingOfficeIcon, ShoppingCartIcon, CalendarDaysIcon, ChevronLeftIcon, ChevronRightIcon, TruckIcon, ShieldCheckIcon, ArrowLeftIcon } from '@heroicons/react/24/outline';
import { CheckBadgeIcon, StarIcon as StarSolid } from '@heroicons/react/24/solid';

interface Product { id: string; name: string; vendor: string; vendorType: string; vendorPhone: string; vendorEmail: string; vendorCity: string; vendorVerified: boolean; vendorLogo?: string; vendorCover?: string; enterpriseId?: string; price: string; unit: string; category: string; city: string; image: string; alt: string; description?: string; stock?: 'En stock' | 'Stock limité' | 'Rupture de stock'; stockQty?: number; rating?: number; reviewCount?: number; images?: {src: string;alt: string;}[]; reviews?: {author: string;rating: number;date: string;comment: string;}[]; vendorSince?: string; vendorProducts?: number; }
interface SellerProfile { name: string; type: string; phone: string; email: string; city: string; verified: boolean; category: string; logoUrl?: string; coverUrl?: string; }
function SellerProfileModal({ seller, onClose, onContact }: {seller: SellerProfile;onClose: () => void;onContact: () => void;}) { const initials = seller.name.split(' ').slice(0, 2).map((w) => w[0]).join('').toUpperCase(); return <div className="fixed inset-0 z-50 flex items-center justify-center p-4 bg-black/70 backdrop-blur-sm" onClick={onClose}><div className="bg-card border border-border rounded-2xl w-full max-w-sm shadow-2xl shadow-black/60" onClick={(e) => e.stopPropagation()}><div className="flex items-center justify-between px-5 py-4 border-b border-border"><h2 className="text-sm font-extrabold text-foreground">Profil du vendeur</h2><button onClick={onClose} className="p-1.5 rounded-lg text-muted-foreground hover:text-foreground hover:bg-secondary transition-colors"><XMarkIcon className="w-4 h-4" /></button></div><div className="p-5"><div className="flex items-center gap-4 mb-5"><div className="w-14 h-14 rounded-2xl bg-primary/15 flex items-center justify-center shrink-0 overflow-hidden">{seller.logoUrl ? <AppImage src={seller.logoUrl} alt={seller.name} width={56} height={56} className="w-full h-full object-cover" /> : <span className="text-lg font-extrabold text-primary">{initials}</span>}</div><div><div className="flex items-center gap-1.5 flex-wrap"><h3 className="text-base font-extrabold text-foreground">{seller.name}</h3>{seller.verified && <CheckBadgeIcon className="w-4 h-4 text-primary shrink-0" />}</div><p className="text-xs text-muted-foreground mt-0.5">{seller.type}</p>{seller.verified && <span className="inline-flex items-center gap-1 mt-1 px-2 py-0.5 bg-primary/10 text-primary text-[10px] font-bold rounded-full border border-primary/20">✓ Vérifié</span>}</div></div><div className="space-y-3"><div className="flex items-center gap-3 p-3 bg-secondary rounded-xl"><MapPinIcon className="w-4 h-4 text-primary shrink-0" /><div><p className="text-[10px] text-muted-foreground uppercase tracking-wide font-semibold">Localisation</p><p className="text-sm font-semibold text-foreground">{seller.city}</p></div></div><div className="flex items-center gap-3 p-3 bg-secondary rounded-xl"><BuildingOfficeIcon className="w-4 h-4 text-primary shrink-0" /><div><p className="text-[10px] text-muted-foreground uppercase tracking-wide font-semibold">Secteur</p><p className="text-sm font-semibold text-foreground">{seller.category}</p></div></div><div className="flex items-center gap-3 p-3 bg-secondary rounded-xl"><PhoneIcon className="w-4 h-4 text-primary shrink-0" /><div><p className="text-[10px] text-muted-foreground uppercase tracking-wide font-semibold">Téléphone</p><p className="text-sm font-semibold text-foreground">{seller.phone}</p></div></div><div className="flex items-center gap-3 p-3 bg-secondary rounded-xl"><EnvelopeIcon className="w-4 h-4 text-primary shrink-0" /><div><p className="text-[10px] text-muted-foreground uppercase tracking-wide font-semibold">Email</p><p className="text-sm font-semibold text-foreground">{seller.email}</p></div></div></div><button onClick={() => onContact()} className="w-full mt-4 py-2.5 bg-primary text-primary-foreground rounded-xl text-sm font-bold hover:bg-primary/90 transition-colors">Contacter le vendeur</button></div></div></div>; }
interface OrderProduct { id: string; name: string; price: string; unit: string; vendor: string; }
function OrderModal({ product, mode, onClose }: {product: OrderProduct;mode: 'reserve' | 'order';onClose: () => void;}) { const [quantity, setQuantity] = useState('1'); const [note, setNote] = useState(''); const [submitted, setSubmitted] = useState(false); const [errorMessage, setErrorMessage] = useState(''); const [saving, setSaving] = useState(false); const handleSubmit = async (e: React.FormEvent) => { e.preventDefault(); setErrorMessage(''); const parsedQuantity = Number.parseInt(quantity, 10); if (!Number.isInteger(parsedQuantity) || parsedQuantity < 1) { setErrorMessage('Veuillez saisir une quantité valide.'); return; } setSaving(true); try { const supabase = createClient(); const { data: { user }, error: userError } = await supabase.auth.getUser(); if (userError || !user) throw new Error('Votre session a expiré. Veuillez vous reconnecter.'); const { data: productRow, error: productError } = await supabase.from('products').select('id,name,price,unit,vendor_id,enterprise_id').eq('id', product.id).single(); if (productError || !productRow) throw new Error('Produit introuvable.'); if (productRow.vendor_id === user.id) throw new Error('Vous ne pouvez pas commander ou réserver votre propre produit.'); if (mode === 'reserve') { const { error } = await supabase.from('reservations').insert({ buyer_id: user.id, seller_id: productRow.vendor_id, product_id: productRow.id, quantity: parsedQuantity, note: note.trim() }); if (error) throw error; } else { const unitPrice = Number(productRow.price) || 0; const { data: order, error: orderError } = await supabase.from('orders').insert({ buyer_id: user.id, seller_id: productRow.vendor_id, total_amount: unitPrice * parsedQuantity, currency: 'CDF', notes: note.trim() }).select('id').single(); if (orderError || !order) throw orderError || new Error('Impossible de créer la commande.'); const { error: itemError } = await supabase.from('order_items').insert({ order_id: order.id, product_id: productRow.id, product_name: productRow.name, product_price: unitPrice, quantity: parsedQuantity, unit: productRow.unit || '', subtotal: unitPrice * parsedQuantity }); if (itemError) throw itemError; } setSubmitted(true); } catch (err) { console.error('Marketplace order/reservation:', err); setErrorMessage(err instanceof Error ? err.message : 'Erreur lors de l’enregistrement.'); } finally { setSaving(false); } }; return <div className="fixed inset-0 z-50 flex items-center justify-center p-4 bg-black/70 backdrop-blur-sm" onClick={onClose}><div className="bg-card border border-border rounded-2xl w-full max-w-sm shadow-2xl shadow-black/60" onClick={(e) => e.stopPropagation()}><div className="flex items-center justify-between px-5 py-4 border-b border-border"><h2 className="text-sm font-extrabold text-foreground">{mode === 'reserve' ? '📅 Réserver ce produit' : '🛒 Commander ce produit'}</h2><button onClick={onClose} className="p-1.5 rounded-lg text-muted-foreground hover:text-foreground hover:bg-secondary transition-colors"><XMarkIcon className="w-4 h-4" /></button></div>{submitted ? <div className="p-6 text-center"><div className="w-14 h-14 rounded-full bg-green-400/15 flex items-center justify-center mx-auto mb-3"><span className="text-2xl">{mode === 'reserve' ? '📅' : '✅'}</span></div><h3 className="text-base font-extrabold text-foreground mb-1">{mode === 'reserve' ? 'Réservation envoyée !' : 'Commande envoyée !'}</h3><p className="text-xs text-muted-foreground mb-4">{mode === 'reserve' ? 'Le vendeur vous contactera pour confirmer la réservation.' : 'Le vendeur a reçu votre commande et vous contactera bientôt.'}</p><button onClick={onClose} className="w-full py-2.5 bg-primary text-primary-foreground rounded-xl text-sm font-bold hover:bg-primary/90 transition-colors">Fermer</button></div> : <form onSubmit={handleSubmit} className="p-5 space-y-4"><div className="bg-secondary rounded-xl p-3"><p className="text-xs text-muted-foreground mb-0.5">Produit</p><p className="text-sm font-bold text-foreground">{product.name}</p><p className="text-xs text-primary font-semibold mt-0.5">{product.price} {product.unit}</p><p className="text-[10px] text-muted-foreground mt-1">Vendeur : {product.vendor}</p></div><div className="flex flex-col gap-1.5"><label className="text-xs font-semibold text-muted-foreground uppercase tracking-wide">Quantité</label><input type="number" min="1" value={quantity} onChange={(e) => setQuantity(e.target.value)} className="bg-secondary border border-border rounded-lg px-3 py-2.5 text-sm text-foreground focus:outline-none focus:border-primary/60 transition-colors" /></div><div className="flex flex-col gap-1.5"><label className="text-xs font-semibold text-muted-foreground uppercase tracking-wide">Note (optionnel)</label><textarea value={note} onChange={(e) => setNote(e.target.value)} placeholder={mode === 'reserve' ? 'Date souhaitée, conditions...' : 'Instructions de livraison, précisions...'} rows={3} className="bg-secondary border border-border rounded-lg px-3 py-2.5 text-sm text-foreground placeholder:text-muted-foreground/50 focus:outline-none focus:border-primary/60 transition-colors resize-none" /> </div>{errorMessage && <p className="text-xs text-red-400">{errorMessage}</p>}<div className="flex gap-3 pt-1"><button type="button" onClick={onClose} className="flex-1 py-2.5 rounded-xl border border-border text-sm font-semibold text-muted-foreground hover:text-foreground hover:border-primary/40 transition-colors">Annuler</button><button type="submit" disabled={saving} className="flex-1 py-2.5 rounded-xl bg-primary text-primary-foreground text-sm font-bold hover:bg-primary/90 transition-colors disabled:opacity-50">{saving ? 'Enregistrement...' : mode === 'reserve' ? 'Réserver' : 'Commander'}</button></div></form>}</div></div>; }
function StarRating({ rating, size = 'sm' }: {rating: number;size?: 'sm' | 'md';}) { const sz = size === 'md' ? 'w-4 h-4' : 'w-3 h-3'; return <div className="flex items-center gap-0.5">{[1,2,3,4,5].map((s) => <StarSolid key={s} className={`${sz} ${s <= Math.round(rating) ? 'text-amber-400' : 'text-muted-foreground/30'}`} />)}</div>; }

function ProductDetailModal({ product, onClose, onOrder, onReserve, onViewSeller }: {product: Product;onClose: () => void;onOrder: () => void;onReserve: () => void;onViewSeller: () => void;}) { const [activeImg, setActiveImg] = useState(0); const images = product.images && product.images.length > 0 ? product.images : [{ src: product.image, alt: product.alt }]; const initials = product.vendor.split(' ').slice(0, 2).map((w) => w[0]).join('').toUpperCase(); const stockColor = product.stock === 'En stock' ? 'text-green-400 bg-green-400/10 border-green-400/20' : product.stock === 'Stock limité' ? 'text-amber-400 bg-amber-400/10 border-amber-400/20' : 'text-red-400 bg-red-400/10 border-red-400/20'; const stockDot = product.stock === 'En stock' ? 'bg-green-400' : product.stock === 'Stock limité' ? 'bg-amber-400' : 'bg-red-400'; return <div className="fixed inset-0 z-50 flex items-center justify-center p-3 sm:p-6 bg-black/75 backdrop-blur-sm" onClick={onClose}><div className="bg-card border border-border rounded-2xl w-full max-w-2xl max-h-[92vh] overflow-y-auto shadow-2xl shadow-black/70" onClick={(e) => e.stopPropagation()}><div className="sticky top-0 z-10 flex items-center justify-between px-5 py-3.5 border-b border-border bg-card/95 backdrop-blur-sm"><button onClick={onClose} className="flex items-center gap-1.5 text-xs text-muted-foreground hover:text-foreground transition-colors"><ArrowLeftIcon className="w-3.5 h-3.5" />Retour au marketplace</button><span className="badge-gold text-[10px]">{product.category}</span></div><div className="p-5 space-y-5"><div className="space-y-2"><div className="relative h-56 sm:h-72 rounded-xl overflow-hidden bg-secondary"><AppImage src={images[activeImg]?.src} alt={images[activeImg]?.alt} fill className="object-cover" sizes="(max-width: 768px) 100vw, 672px" />{images.length > 1 && <><button onClick={() => setActiveImg((prev) => (prev - 1 + images.length) % images.length)} className="absolute left-2 top-1/2 -translate-y-1/2 w-8 h-8 rounded-full bg-black/50 flex items-center justify-center text-white hover:bg-black/70 transition-colors"><ChevronLeftIcon className="w-4 h-4" /></button><button onClick={() => setActiveImg((prev) => (prev + 1) % images.length)} className="absolute right-2 top-1/2 -translate-y-1/2 w-8 h-8 rounded-full bg-black/50 flex items-center justify-center text-white hover:bg-black/70 transition-colors"><ChevronRightIcon className="w-4 h-4" /></button><div className="absolute bottom-2 left-1/2 -translate-x-1/2 flex gap-1">{images.map((_, i) => <button key={i} onClick={() => setActiveImg(i)} className={`w-1.5 h-1.5 rounded-full transition-all ${i === activeImg ? 'bg-white w-4' : 'bg-white/50'}`} />)}</div></>}</div>{images.length > 1 && <div className="flex gap-2 overflow-x-auto pb-1">{images.map((img, i) => <button key={i} onClick={() => setActiveImg(i)} className={`relative w-14 h-14 rounded-lg overflow-hidden shrink-0 border-2 transition-all ${i === activeImg ? 'border-primary' : 'border-border hover:border-primary/40'}`}><AppImage src={img.src} alt={img.alt} fill className="object-cover" sizes="56px" /></button>)}</div>}</div><div><h1 className="text-xl font-extrabold text-foreground leading-tight mb-2">{product.name}</h1><div className="flex items-center flex-wrap gap-3 mb-3"><div className="flex items-baseline gap-1"><span className="text-2xl font-extrabold text-primary">{product.price}</span><span className="text-sm text-muted-foreground">{product.unit}</span></div>{product.stock && <span className={`inline-flex items-center gap-1.5 px-2.5 py-1 rounded-full text-xs font-bold border ${stockColor}`}><span className={`w-1.5 h-1.5 rounded-full ${stockDot}`} />{product.stock}{product.stock === 'Stock limité' && product.stockQty && <span className="opacity-70">({product.stockQty} restants)</span>}</span>}</div>{product.rating && <div className="flex items-center gap-2"><StarRating rating={product.rating} size="md" /><span className="text-sm font-bold text-foreground">{product.rating.toFixed(1)}</span><span className="text-xs text-muted-foreground">({product.reviewCount} avis)</span></div>}</div>{product.description && <div><h2 className="text-sm font-extrabold text-foreground mb-2 uppercase tracking-wide">Description</h2><p className="text-sm text-muted-foreground leading-relaxed">{product.description}</p></div>}<div className="grid grid-cols-2 gap-2"><div className="flex items-center gap-2.5 p-3 bg-secondary rounded-xl"><TruckIcon className="w-4 h-4 text-primary shrink-0" /><div><p className="text-[10px] text-muted-foreground font-semibold uppercase tracking-wide">Livraison</p><p className="text-xs font-bold text-foreground">Disponible</p></div></div><div className="flex items-center gap-2.5 p-3 bg-secondary rounded-xl"><ShieldCheckIcon className="w-4 h-4 text-primary shrink-0" /><div><p className="text-[10px] text-muted-foreground font-semibold uppercase tracking-wide">Garantie</p><p className="text-xs font-bold text-foreground">Qualité assurée</p></div></div></div><div className="border border-border rounded-xl overflow-hidden"><div className="px-4 py-3 border-b border-border bg-secondary/50"><h2 className="text-sm font-extrabold text-foreground uppercase tracking-wide">Vendeur</h2></div><div className="p-4"><div className="flex items-center gap-3 mb-3"><div className="w-12 h-12 rounded-xl bg-primary/15 flex items-center justify-center shrink-0 overflow-hidden">{product.vendorLogo ? <AppImage src={product.vendorLogo} alt={product.vendor} width={48} height={48} className="w-full h-full object-cover" /> : <span className="text-base font-extrabold text-primary">{initials}</span>}</div><div className="flex-1 min-w-0"><div className="flex items-center gap-1.5 flex-wrap"><span className="text-sm font-extrabold text-foreground truncate">{product.vendor}</span>{product.vendorVerified && <CheckBadgeIcon className="w-4 h-4 text-primary shrink-0" />}</div><p className="text-xs text-muted-foreground">{product.vendorType}</p></div>{product.vendorVerified && <span className="shrink-0 px-2 py-0.5 bg-primary/10 text-primary text-[10px] font-bold rounded-full border border-primary/20">✓ Vérifié</span>}</div><div className="grid grid-cols-3 gap-2 mb-3"><div className="text-center p-2 bg-secondary rounded-lg"><p className="text-sm font-extrabold text-foreground">{product.vendorSince || '—'}</p><p className="text-[10px] text-muted-foreground">Depuis</p></div><div className="text-center p-2 bg-secondary rounded-lg"><p className="text-sm font-extrabold text-foreground">{product.vendorProducts || '—'}</p><p className="text-[10px] text-muted-foreground">Produits</p></div><div className="text-center p-2 bg-secondary rounded-lg"><MapPinIcon className="w-3.5 h-3.5 text-primary mx-auto mb-0.5" /><p className="text-[10px] text-muted-foreground">{product.vendorCity}</p></div></div><button onClick={onViewSeller} className="w-full py-2 rounded-lg border border-primary/40 text-primary text-xs font-bold hover:bg-primary/10 transition-colors">Voir le profil complet</button></div></div>{product.reviews && product.reviews.length > 0 && <div><div className="flex items-center justify-between mb-3"><h2 className="text-sm font-extrabold text-foreground uppercase tracking-wide">Avis clients</h2><div className="flex items-center gap-1.5"><StarRating rating={product.rating || 0} /><span className="text-xs text-muted-foreground">{product.reviewCount} avis</span></div></div><div className="space-y-3">{product.reviews.map((review, idx) => <div key={idx} className="p-3.5 bg-secondary rounded-xl"><div className="flex items-start justify-between gap-2 mb-1.5"><div><p className="text-xs font-bold text-foreground">{review.author}</p><p className="text-[10px] text-muted-foreground">{review.date}</p></div><StarRating rating={review.rating} /></div><p className="text-xs text-muted-foreground leading-relaxed">{review.comment}</p></div>)}</div></div>}<div className="flex gap-3 pt-1 pb-1"><button onClick={onReserve} disabled={product.stock === 'Rupture de stock'} className="flex-1 flex items-center justify-center gap-2 py-3 rounded-xl border border-primary/40 text-primary text-sm font-bold hover:bg-primary/10 transition-colors disabled:opacity-40 disabled:cursor-not-allowed"><CalendarDaysIcon className="w-4 h-4" />Réserver</button><button onClick={onOrder} disabled={product.stock === 'Rupture de stock'} className="flex-1 flex items-center justify-center gap-2 py-3 rounded-xl bg-primary text-primary-foreground text-sm font-bold hover:bg-primary/90 transition-colors disabled:opacity-40 disabled:cursor-not-allowed"><ShoppingCartIcon className="w-4 h-4" />Ajouter au panier</button></div></div></div></div>; }

export default function MarketplaceContent() { const [products, setProducts] = useState<Product[]>([]); useEffect(() => { const supabase = createClient(); let mounted = true; const load = async () => { const { data, error } = await supabase.from('products').select('id,name,description,price,unit,category,city,image_url,alt_text,stock_status,stock_qty,rating,review_count,vendor_id,vendor_name,vendor_type,vendor_phone,vendor_email,vendor_city,vendor_verified,vendor_since,enterprise_id,created_at,enterprises:enterprise_id(name,logo_url,cover_url,is_verified,founded_year,city)').eq('is_active', true).order('created_at', { ascending: false }); if (!mounted) return; if (error) { console.error('Marketplace:', error.message); setProducts([]); return; } const rows = (data || []) as any[]; const counts = new Map<string, number>(); rows.forEach(r => { if (r.vendor_id) counts.set(r.vendor_id, (counts.get(r.vendor_id) || 0) + 1); }); setProducts(rows.map(r => ({ id:r.id, name:r.name, vendor:r.vendor_name || r.enterprises?.name || 'Vendeur', vendorType:r.vendor_type || 'Entreprise', vendorPhone:r.vendor_phone || '', vendorEmail:r.vendor_email || '', vendorCity:r.vendor_city || r.enterprises?.city || r.city || '', vendorVerified:Boolean(r.vendor_verified || r.enterprises?.is_verified), price:r.price, unit:r.unit ? (r.unit.startsWith('/') ? r.unit : '/ ' + r.unit) : '', category:r.category || 'Autres', city:r.city || r.vendor_city || '', image:r.image_url || '', alt:r.alt_text || r.name, description:r.description || undefined, stock:r.stock_status || 'En stock', stockQty:r.stock_qty ?? 0, rating:r.rating == null ? undefined : Number(r.rating), reviewCount:r.review_count ?? 0, vendorSince:r.vendor_since || (r.enterprises?.founded_year ? String(r.enterprises.founded_year) : ''), vendorProducts:r.vendor_id ? counts.get(r.vendor_id) || 0 : undefined, vendorLogo:r.enterprises?.logo_url || undefined, vendorCover:r.enterprises?.cover_url || undefined, enterpriseId:r.enterprise_id || undefined }))); }; void load(); const channel=supabase.channel('marketplace-products').on('postgres_changes',{event:'*',schema:'public',table:'products'},()=>void load()).subscribe(); return()=>{mounted=false;void supabase.removeChannel(channel);}; }, []); const allCategories=useMemo(()=>{const m=new Map<string,number>(); products.forEach(p=>m.set(p.category,(m.get(p.category)||0)+1)); return [{label:'Toutes catégories',count:products.length},...Array.from(m.entries()).sort(([a],[b])=>a.localeCompare(b,'fr')).map(([label,count])=>({label,count}))];},[products]); const allCities=useMemo(()=>['Toutes les villes',...Array.from(new Set(products.map(p=>p.city).filter(Boolean))).sort((a,b)=>a.localeCompare(b,'fr'))],[products]); const { isLoggedIn } = useAuth(); const [selectedCategory, setSelectedCategory] = useState('Toutes catégories'); const [selectedCity, setSelectedCity] = useState('Toutes les villes'); const [searchQuery, setSearchQuery] = useState(''); const [sidebarOpen, setSidebarOpen] = useState(false); const [sellerModal, setSellerModal] = useState<SellerProfile | null>(null); const [orderModal, setOrderModal] = useState<{product: OrderProduct;mode: 'reserve' | 'order';} | null>(null); const [authGuard, setAuthGuard] = useState<'order' | 'reserve' | 'message' | 'contact' | null>(null); const [detailProduct, setDetailProduct] = useState<Product | null>(null); const handleProtectedAction = (action: 'order' | 'reserve' | 'message' | 'contact', fn: () => void) => { if (!isLoggedIn) setAuthGuard(action); else fn(); }; const filtered = products.filter((p) => { const matchCat = selectedCategory === 'Toutes catégories' || p?.category === selectedCategory; const matchCity = selectedCity === 'Toutes les villes' || p?.city === selectedCity; const matchSearch = !searchQuery || p?.name?.toLowerCase()?.includes(searchQuery?.toLowerCase()) || p?.vendor?.toLowerCase()?.includes(searchQuery?.toLowerCase()); return matchCat && matchCity && matchSearch; }); return <div className="min-h-screen bg-background"><div className="border-b border-border px-4 py-3"><div className="max-w-7xl mx-auto flex items-center gap-2 text-sm text-muted-foreground"><Link href="/" className="hover:text-primary transition-colors">Accueil</Link><span>/</span><span className="text-foreground font-medium">Marketplace</span></div></div><div className="max-w-7xl mx-auto px-4 py-6 flex gap-6"><aside className="hidden lg:flex flex-col gap-6 w-56 shrink-0"><div className="bg-card border border-border rounded-xl overflow-hidden"><div className="px-4 py-3 border-b border-border"><h3 className="font-bold text-sm text-foreground">Catégories</h3></div><div className="p-2">{allCategories?.map((cat) => <button key={cat?.label} onClick={() => setSelectedCategory(cat?.label)} className={`w-full flex items-center justify-between px-3 py-2 rounded-lg text-sm transition-all ${selectedCategory === cat?.label ? 'bg-primary/15 text-primary font-semibold' : 'text-muted-foreground hover:bg-secondary hover:text-foreground'}`}><span>{cat?.label}</span>{cat?.count > 0 && <span className={`text-[11px] ${selectedCategory === cat?.label ? 'text-primary' : 'text-muted-foreground'}`}>{cat?.count}</span>}</button>)}</div></div><div className="bg-card border border-border rounded-xl overflow-hidden"><div className="px-4 py-3 border-b border-border"><h3 className="font-bold text-sm text-foreground">Localisation</h3></div><div className="p-2">{allCities?.map((city) => <button key={city} onClick={() => setSelectedCity(city)} className={`w-full flex items-center gap-2 px-3 py-2 rounded-lg text-sm transition-all ${selectedCity === city ? 'bg-primary/15 text-primary font-semibold' : 'text-muted-foreground hover:bg-secondary hover:text-foreground'}`}><MapPinIcon className="w-3.5 h-3.5" /><span>{city}</span></button>)}</div></div></aside><div className="flex-1 min-w-0"><div className="flex items-center gap-3 mb-6"><div className="flex-1 flex items-center gap-2 bg-card border border-border rounded-lg px-3 py-2.5"><MagnifyingGlassIcon className="w-4 h-4 text-muted-foreground shrink-0" /><input type="text" placeholder="Rechercher un produit..." value={searchQuery} onChange={(e) => setSearchQuery(e?.target?.value)} className="flex-1 bg-transparent text-sm text-foreground placeholder-muted-foreground outline-none" />{searchQuery && <button onClick={() => setSearchQuery('')}><XMarkIcon className="w-4 h-4 text-muted-foreground hover:text-foreground" /></button>}</div><select value={selectedCity} onChange={(e) => setSelectedCity(e?.target?.value)} className="hidden sm:block bg-card border border-border rounded-lg px-3 py-2.5 text-sm text-foreground outline-none cursor-pointer">{allCities?.map((c) => <option key={c} value={c}>{c}</option>)}</select><button onClick={() => setSidebarOpen(!sidebarOpen)} className="lg:hidden flex items-center gap-2 bg-primary text-primary-foreground px-4 py-2.5 rounded-lg text-sm font-semibold"><AdjustmentsHorizontalIcon className="w-4 h-4" />Filtrer</button></div>{sidebarOpen && <div className="lg:hidden fixed inset-0 z-50 bg-black/60" onClick={() => setSidebarOpen(false)}><div className="absolute left-0 top-0 bottom-0 w-72 bg-card border-r border-border p-4 overflow-y-auto" onClick={(e) => e?.stopPropagation()}><div className="flex items-center justify-between mb-4"><h3 className="font-bold text-foreground">Filtres</h3><button onClick={() => setSidebarOpen(false)}><XMarkIcon className="w-5 h-5 text-muted-foreground" /></button></div><div className="mb-4"><h4 className="text-sm font-semibold text-foreground mb-2">Catégories</h4>{allCategories?.map((cat) => <button key={cat?.label} onClick={() => {setSelectedCategory(cat?.label);setSidebarOpen(false);}} className={`w-full flex items-center justify-between px-3 py-2 rounded-lg text-sm mb-1 transition-all ${selectedCategory === cat?.label ? 'bg-primary/15 text-primary font-semibold' : 'text-muted-foreground hover:bg-secondary'}`}><span>{cat?.label}</span>{cat?.count > 0 && <span className="text-[11px]">{cat?.count}</span>}</button>)}</div><div><h4 className="text-sm font-semibold text-foreground mb-2">Villes</h4>{allCities?.map((city) => <button key={city} onClick={() => {setSelectedCity(city);setSidebarOpen(false);}} className={`w-full flex items-center gap-2 px-3 py-2 rounded-lg text-sm mb-1 transition-all ${selectedCity === city ? 'bg-primary/15 text-primary font-semibold' : 'text-muted-foreground hover:bg-secondary'}`}><MapPinIcon className="w-3.5 h-3.5" /><span>{city}</span></button>)}</div></div></div>}{<div className="flex items-center justify-between mb-4"><p className="text-sm text-muted-foreground"><span className="text-foreground font-semibold">{filtered?.length}</span> produits trouvés</p><div className="flex items-center gap-2 text-sm text-muted-foreground">{selectedCategory !== 'Toutes catégories' && <span className="badge-gold cursor-pointer" onClick={() => setSelectedCategory('Toutes catégories')}>{selectedCategory} ×</span>}</div></div>}{filtered?.length === 0 ? <div className="text-center py-20 text-muted-foreground"><p className="text-lg font-semibold mb-2">Aucun produit trouvé</p><p className="text-sm">Essayez de modifier vos filtres</p></div> : <div className="grid grid-cols-2 sm:grid-cols-3 xl:grid-cols-4 gap-4">{filtered?.map((product) => <div key={product?.id} className="product-card flex flex-col"><button className="relative h-36 overflow-hidden w-full text-left" onClick={() => setDetailProduct(product)}><AppImage src={product?.image} alt={product?.alt} fill className="object-cover hover:scale-105 transition-transform duration-300" sizes="(max-width: 768px) 50vw, 25vw" /><div className="absolute top-2 right-2"><span className="badge-gold text-[10px]">{product?.category}</span></div>{product.stock === 'Stock limité' && <div className="absolute bottom-2 left-2"><span className="px-1.5 py-0.5 bg-amber-400/90 text-black text-[9px] font-bold rounded">Stock limité</span></div>}{product.stock === 'Rupture de stock' && <div className="absolute inset-0 bg-black/50 flex items-center justify-center"><span className="px-2 py-1 bg-red-500/90 text-white text-[10px] font-bold rounded">Rupture de stock</span></div>}</button><div className="p-3 flex flex-col flex-1"><button onClick={() => setDetailProduct(product)} className="text-sm font-bold text-foreground leading-tight mb-1 line-clamp-2 text-left hover:text-primary transition-colors">{product?.name}</button>{product.rating && <div className="flex items-center gap-1 mb-1.5"><StarRating rating={product.rating} /><span className="text-[10px] text-muted-foreground">({product.reviewCount})</span></div>}<button onClick={() => setSellerModal({name: product.vendor,type: product.vendorType,phone: product.vendorPhone,email: product.vendorEmail,city: product.vendorCity,verified: product.vendorVerified,category: product.category,logoUrl: product.vendorLogo,coverUrl: product.vendorCover})} className="flex items-center gap-1 mb-1.5 group/seller w-full text-left"><CheckBadgeIcon className="w-3 h-3 text-primary shrink-0" /><span className="text-[11px] text-muted-foreground group-hover/seller:text-primary transition-colors font-medium underline-offset-2 group-hover/seller:underline truncate">{product?.vendor}</span></button><div className="flex items-center gap-1 mb-3"><MapPinIcon className="w-3 h-3 text-muted-foreground" /><p className="text-[11px] text-muted-foreground">{product?.city}</p></div><div className="flex items-baseline gap-1 mb-3"><span className="text-sm font-extrabold text-primary">{product?.price}</span><span className="text-[10px] text-muted-foreground">{product?.unit}</span></div><div className="flex gap-1.5 mt-auto"><button onClick={() => handleProtectedAction('reserve', () => setOrderModal({product:{id:product.id,name:product.name,price:product.price,unit:product.unit,vendor:product.vendor},mode:'reserve'}))} className="flex-1 flex items-center justify-center gap-1 py-1.5 rounded-lg border border-primary/40 text-primary text-[11px] font-semibold hover:bg-primary/10 transition-colors"><CalendarDaysIcon className="w-3 h-3" />Réserver</button><button onClick={() => handleProtectedAction('order', () => setOrderModal({product:{id:product.id,name:product.name,price:product.price,unit:product.unit,vendor:product.vendor},mode:'order'}))} className="flex-1 flex items-center justify-center gap-1 py-1.5 rounded-lg bg-primary text-primary-foreground text-[11px] font-semibold hover:bg-primary/90 transition-colors"><ShoppingCartIcon className="w-3 h-3" />Commander</button></div></div></div>)}</div>}</div></div></div>{detailProduct && <ProductDetailModal product={detailProduct} onClose={() => setDetailProduct(null)} onOrder={() => handleProtectedAction('order', () => {setOrderModal({product:{id:detailProduct.id,name:detailProduct.name,price:detailProduct.price,unit:detailProduct.unit,vendor:detailProduct.vendor},mode:'order'});setDetailProduct(null);})} onReserve={() => handleProtectedAction('reserve', () => {setOrderModal({product:{id:detailProduct.id,name:detailProduct.name,price:detailProduct.price,unit:detailProduct.unit,vendor:detailProduct.vendor},mode:'reserve'});setDetailProduct(null);})} onViewSeller={() => {setSellerModal({name:detailProduct.vendor,type:detailProduct.vendorType,phone:detailProduct.vendorPhone,email:detailProduct.vendorEmail,city:detailProduct.vendorCity,verified:detailProduct.vendorVerified,category:detailProduct.category});setDetailProduct(null);}} />}{sellerModal && <SellerProfileModal seller={sellerModal} onClose={() => setSellerModal(null)} onContact={() => handleProtectedAction('contact', () => setSellerModal(null))} />}{orderModal && <OrderModal product={orderModal.product} mode={orderModal.mode} onClose={() => setOrderModal(null)} />}</div>; }