# Supabase

Apply migrations in `supabase/migrations` to the linked Supabase project before testing the database-backed dashboard.

The MVP core uses Supabase Auth (`auth.users`) plus application tables `profiles`, `enterprises`, and `products`.
